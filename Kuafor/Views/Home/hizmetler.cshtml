@model IEnumerable<Kuafor.Models.islemmodel>
@{
   
    Layout = "~/Views/Shared/Layout2.cshtml";
}



<body style="background-image:url(/kuaför/image/hizmetarka.png)">
    <form method="post" class="form-inline">
    </form>
    </div>
    </nav>
    <div class="col-lg-12"><hr /> </div>
    <div style=" background-color:white;margin-left:350px; margin-top:70px;" class="wrapper4">
        <form class="form-inline">
            <h2>HİZMETLERİM</h2>
            <table class="table">
                <tr>
                    <th>İŞLEMLER</th>
                    <th>ÜCRET</th>

                    <!-- Diğer sütunlar -->
                </tr>

                @foreach (var veri in Model)
                {


                    using (Html.BeginForm("hizmetler", "Home", new { id = veri.işlemlervm.ID }, FormMethod.Post))
                    {
                    <tr>
                        <td>@veri.işlemlervm.işlem_türü</td>
                        <td>
                            <div style="margin-left: 1px; " class="input-box">
                                <input  class="randevuişlem"style="font-size:17px;width:70px; background-color:white;" name="işlemfiyat" type="text" value="@veri.işlemücretvm.ücret "><p class="tl">TL</p>
                        <td><a href="/Home/silişlem/@veri.işlemlervm.ID" class="btn btn-danger">Sil</a>
                        <td>  <button  style="color:forestgreen;" type="submit" class="btn ">Güncelle</button></td></td>
                       
        </div>
        </td>

        <!-- Diğer sütunlar -->
        </tr>

                    }
                }

                </table>

                @*@foreach (var item in Model)

                    {
                        <div class="col-lg-12"><hr /> </div>
                        <div class="container">

                            <input style="background-color:transparent;" class="input-box3" name="işlem" type="text" value=@item.işlemlervm.işlem_türü required>
                            <input style="background-color:transparent;" class="input-box4" name="işlemfiyat" type="number" value=@item.işlemücretvm.ücret required>



                        </div>

                    }*@

                </div>
                </form>
                </div>

            </body>